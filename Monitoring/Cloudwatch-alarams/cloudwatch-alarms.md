## Awareness Layer – Alerting

CloudWatch alarms are configured to detect abnormal
resource behavior such as sustained high CPU usage.

Target Tracking automatically manages alarms at the
Auto Scaling Group level, ensuring reliable detection
even when instances change dynamically.
